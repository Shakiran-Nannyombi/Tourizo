# Production Environment Variables
# Generate a secret key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Database URL
# For local SQLite (default if not set):
# DATABASE_URL=sqlite:///instance/travel_app.db

# For Supabase PostgreSQL (Required for Render):
# Connection Pooling (Recommended for Transaction mode):
# DATABASE_URL=postgresql://postgres.yourproject:password@aws-0-region.pooler.supabase.com:6543/postgres

# Direct Connection (Session mode):
DATABASE_URL=postgresql://postgres.yourproject:password@db.yourproject.supabase.co:5432/postgres

# Flask Environment
FLASK_APP=run.py
FLASK_ENV=production

# Mail Configuration (Optional)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=noreply@tourizo.com

# Admin Email
ADMIN_EMAIL=admin@tourizo.com

# Upload Folder
UPLOAD_FOLDER=static/uploads

